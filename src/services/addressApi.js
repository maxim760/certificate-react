import { $host } from '.'

export const addressApi = {
  async getPlaces(query) {
    try {
      const data = []
      return data
    } catch (error) {
      throw new Error(error.message)
    }
  },
}
